/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';

// App name matches the 'name' in app.config.js
const appName = 'Pruuf';

AppRegistry.registerComponent(appName, () => App);
